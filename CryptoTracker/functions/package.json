{
  "name": "cryptotracker-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": { "node": "18" },
  "scripts": {
    "build": "tsc",
    "lint": "echo \"skip\"",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.0",
    "@sendgrid/mail": "^8.1.4",
    "twilio": "^4.25.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
